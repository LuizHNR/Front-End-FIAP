export type CabecalhoProps ={
    tituloProps:string;
}

export type dadosAluno = {
    nome:string;
    rm:string;
}

export type chave = {
    id:number
}

export type RodapeProps = {
    textoProps: string;
}